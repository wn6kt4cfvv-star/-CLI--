# Тесты

- test_valid.cfg — корректная конфигурация
- test_invalid.cfg — ошибка (необъявленная константа)

Запуск:
```bash
python src/config_tool.py --input tests/test_valid.cfg --output out.json